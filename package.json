{
  "name": "heykyy-monorepo",
  "private": true,
  "type": "module",
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "scripts": {
    "dev": "turbo dev",
    "build": "turbo build",
    "build:apps": "turbo build --filter=\"./apps/**\"",
    "build:packages": "turbo build --filter=\"./packages/**\"",
    "clean": "turbo run clean",
    "lint": "turbo run lint",
    "bootstrap": "npm install && turbo prune --force"
  },
  "devDependencies": {
    "eslint": "^9.39.1"
  }
}
